<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Blogi</title>
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@600;700&display=swap');

    body {
      margin: 0;
      padding: 0;
      height: 100vh;
      background: linear-gradient(135deg, #667eea, #764ba2);
      font-family: 'Poppins', sans-serif;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      animation: fadeIn 1s ease-in;
      position: relative;
      overflow: hidden;
    }

    .logo {
      font-size: 4.5rem;
      font-weight: 700;
      color: #ffffff;
      position: relative;
      display: flex;
      align-items: flex-end;
      opacity: 0;
      animation: logoFade 1.2s ease-out forwards 0.3s;
      text-shadow: 2px 2px 10px rgba(0, 0, 0, 0.25);
      letter-spacing: 1px;
      z-index: 2;
    }

    .i-container {
      position: relative;
      display: inline-block;
      margin-left: 6px;
    }

    .dot {
      width: 13px;
      height: 13px;
      background-color: #ffd166;
      border-radius: 50%;
      position: absolute;
      top: -18px;
      left: 6px;
      opacity: 0;
      animation: dropDot 1s ease-out forwards 1.1s, pulse 2s infinite ease-in-out 2.2s;
      box-shadow: 0 0 10px rgba(255, 209, 102, 0.8);
    }

    .tagline {
      margin-top: 12px;
      font-size: 1.1rem;
      color: #f8f8f8;
      opacity: 0;
      animation: taglineFade 1s ease-out forwards 1.8s;
      letter-spacing: 1px;
      z-index: 2;
    }

    .wave {
      position: absolute;
      bottom: 0;
      left: 0;
      width: 100%;
      z-index: 1;
    }

    @keyframes dropDot {
      0% {
        transform: translateY(-40px) scale(0.6);
        opacity: 0;
      }
      100% {
        transform: translateY(0) scale(1);
        opacity: 1;
      }
    }

    @keyframes pulse {
      0%, 100% {
        box-shadow: 0 0 0px rgba(255, 209, 102, 0.8);
      }
      50% {
        box-shadow: 0 0 14px rgba(255, 209, 102, 0.9);
      }
    }

    @keyframes logoFade {
      to {
        opacity: 1;
      }
    }

    @keyframes taglineFade {
      from {
        transform: translateY(10px);
        opacity: 0;
      }
      to {
        transform: translateY(0);
        opacity: 1;
      }
    }

    @keyframes fadeIn {
      from {
        opacity: 0;
      }
      to {
        opacity: 1;
      }
    }
  </style>

  <script>
    setTimeout(() => {
      document.body.style.transition = 'opacity 0.8s ease';
      document.body.style.opacity = '0';
      setTimeout(() => {
        window.location.href = "/home"; 
      }, 800);
    }, 3000);
  </script>
</head>
<body>
  <div class="logo">
    BLOG
    <span class="i-container">
      <span class="dot"></span>
      l
    </span>
  </div>
  <p class="tagline">Write. Share. Repeat.</p>

  <svg class="wave" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1440 320">
    <path fill="#ffffff33" fill-opacity="1"
      d="M0,160L40,149.3C80,139,160,117,240,106.7C320,96,400,96,480,117.3C560,139,640,181,720,181.3C800,181,880,139,960,138.7C1040,139,1120,181,1200,181.3C1280,181,1360,139,1400,117.3L1440,96L1440,320L1400,320C1360,320,1280,320,1200,320C1120,320,1040,320,960,320C880,320,800,320,720,320C640,320,560,320,480,320C400,320,320,320,240,320C160,320,80,320,40,320L0,320Z">
    </path>
  </svg>
</body>
</html>
